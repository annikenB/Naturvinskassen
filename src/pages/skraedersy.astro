---
import MainLayout from "../layouts/MainLayout.astro";
import Button from "../components/Button.astro";
import Link from "../components/Link.astro";
import { Image } from "astro:assets";
import pil from "../assets/images/v.webp";
import sy from "../assets/images/sy.webp";
---

<MainLayout title="Skræddersy">
  <h1 class="text-center">Skræddersy dit abonnement</h1>

  <div class="text-center">
    <p class="inline-flex items-center my-voress">
      <a href="/bestil" class="font-bold">Vælg</a>

      <Image src={pil} alt="pil" class="mx-voress w-4 opacity-100 -rotate-90" />

      <a href="/betal" class="font-regular">Betal</a>
    </p>
  </div>
  <section class="grid gris-cols-2">
    <article class="p-4 mx-margin w-[120] bg-blaa-300 rounded-card">
      <div></div>
      <div class="p-4">
        <h3 class="text-center">Din kasse Lige som du kan lide det</h3>
        <p class="p-voresxs text-center">
          Tilføj de typer af naturvin som du ønsker i dit månedlige abonnement.
          Du vil modtage kassen pr. d. første hver måned. - Naturvin lige til
          døre!
        </p>
      </div>

      <div class="m-voresxs">
        <div class="flex items-center justify-between py-2">
          <span class="text-lg font-medium">Hvid</span>
          <div class="flex items-center space-x-2">
            <form class="max-w-xs mx-auto bg-white">
              <div class="relative flex items-center justify-center space-x-2">
                <button
                  type="button"
                  id="decrement-button"
                  class="flex-shrink-0 font-bold py-2 px-3 rounded focus:outline-none"
                >
                  &minus;
                </button>

                <input
                  type="text"
                  id="counter-input"
                  value="0"
                  class="w-12 text-center bg-blaa-300 border-none focus:outline-none"
                />

                <button
                  type="button"
                  id="increment-button"
                  class="flex-shrink-0 font-bold py-2 px-3 focus:outline-none"
                >
                  &plus;
                </button>
              </div>
            </form>
          </div>
        </div>

        <div
          class="flex items-center justify-between border-t border-b-1 border-gray-300 py-2"
        >
          <span class="text-lg font-medium">Rød</span>
          <div class="flex items-center space-x-2">
            <form class="max-w-xs mx-auto bg-white">
              <div class="relative flex items-center justify-center space-x-2">
                <button
                  type="button"
                  id="decrement-button"
                  class="flex-shrink-0 font-bold py-2 px-3 rounded focus:outline-none"
                >
                  &minus;
                </button>

                <input
                  type="text"
                  id="counter-input"
                  value="1"
                  class="w-12 text-center bg-blaa-300 border-none focus:outline-none"
                />

                <button
                  type="button"
                  id="increment-button"
                  class="flex-shrink-0 font-bold py-2 px-3 focus:outline-none"
                >
                  &plus;
                </button>
              </div>
            </form>
          </div>
        </div>

        <div
          class="flex items-center justify-between border-t border-b-1 border-gray-300 py-2"
        >
          <span class="text-lg font-medium">Orange</span>
          <div class="flex items-center space-x-2">
            <form class="max-w-xs mx-auto bg-white">
              <div class="relative flex items-center justify-center space-x-2">
                <button
                  type="button"
                  id="decrement-button"
                  class="flex-shrink-0 font-bold py-2 px-3 rounded focus:outline-none"
                >
                  &minus;
                </button>

                <input
                  type="text"
                  id="counter-input"
                  value="1"
                  class="w-12 text-center bg-blaa-300 border-none focus:outline-none"
                />

                <button
                  type="button"
                  id="increment-button"
                  class="flex-shrink-0 font-bold py-2 px-3 focus:outline-none"
                >
                  &plus;
                </button>
              </div>
            </form>
          </div>
        </div>

        <div
          class="flex items-center justify-between border-t border-b-1 border-gray-300 py-2"
        >
          <span class="text-lg font-medium">Rose</span>
          <div class="flex items-center space-x-2">
            <form class="max-w-xs mx-auto bg-white">
              <div class="relative flex items-center justify-center space-x-2">
                <button
                  type="button"
                  id="decrement-button"
                  class="flex-shrink-0 font-bold py-2 px-3 rounded focus:outline-none"
                >
                  &minus;
                </button>

                <input
                  type="text"
                  id="counter-input"
                  value="0"
                  class="w-12 text-center bg-blaa-300 border-none focus:outline-none"
                />

                <button
                  type="button"
                  id="increment-button"
                  class="flex-shrink-0 font-bold py-2 px-3 focus:outline-none"
                >
                  &plus;
                </button>
              </div>
            </form>
          </div>
        </div>

        <div
          class="flex items-center justify-between border-t border-b-1 border-gray-300 py-2"
        >
          <span class="text-lg font-medium">Bobler</span>
          <div class="flex items-center space-x-2">
            <form class="max-w-xs mx-auto bg-white">
              <div class="relative flex items-center justify-center space-x-2">
                <button
                  type="button"
                  id="decrement-button"
                  class="flex-shrink-0 font-bold py-2 px-3 rounded focus:outline-none"
                >
                  &minus;
                </button>

                <input
                  type="text"
                  id="counter-input"
                  value="1"
                  class="w-12 text-center bg-blaa-300 border-none focus:outline-none"
                />

                <button
                  type="button"
                  id="increment-button"
                  class="flex-shrink-0 font-bold py-2 px-3 focus:outline-none"
                >
                  &plus;
                </button>
              </div>
            </form>
          </div>
        </div>

        <div
          class="flex items-center justify-between border-t border-b-1 border-gray-300 py-2"
        >
          <span class="text-lg font-medium">Suprise mig</span>
          <div class="flex items-center space-x-2">
            <form class="max-w-xs mx-auto bg-white">
              <div class="relative flex items-center justify-center space-x-2">
                <button
                  type="button"
                  id="decrement-button"
                  class="flex-shrink-0 font-bold py-2 px-3 rounded focus:outline-none"
                >
                  &minus;
                </button>

                <input
                  type="text"
                  id="counter-input"
                  value="1"
                  class="w-12 text-center bg-blaa-300 border-none focus:outline-none"
                />

                <button
                  type="button"
                  id="increment-button"
                  class="flex-shrink-0 font-bold py-2 px-3 focus:outline-none"
                >
                  &plus;
                </button>
              </div>
            </form>
          </div>
        </div>

        <div
          class="flex items-center justify-between border-gray-500 mt-6 pt-4"
        >
          <span class="navigation">Total</span>
          <span class="navigation">600 DKK</span>
        </div>
      </div>
    </article>
    <article>
      <div class="mx-auto max-w-44 mt-voress">
        <Image src={sy} alt="mand der syr" class="" />
      </div>
    </article>
  </section>
  <div class="mt-voresxs mx-auto md:block hidden">
    <a href="/bestil">
      <Button variant="primary">Tilføj til kurv</Button>
    </a>
  </div>
  <div class="mt-voresxs mx-auto md:hidden block">
    <a href="/bestil">
      <Button variant="mobileprimary">Tilføj til kurv</Button>
    </a>
  </div>
  <script>
    document.addEventListener("DOMContentLoaded", function () {
      const decrementButton = document.getElementById("decrement-button");
      const incrementButton = document.getElementById("increment-button");
      const counterInput = document.getElementById("counter-input");

      decrementButton.addEventListener("click", () => {
        let currentValue = parseInt(counterInput.value) || 0;
        if (currentValue > 0) {
          counterInput.value = currentValue - 1;
        }
      });

      incrementButton.addEventListener("click", () => {
        let currentValue = parseInt(counterInput.value) || 0;
        counterInput.value = currentValue + 1;
      });

      counterInput.addEventListener("input", (e) => {
        e.target.value = e.target.value.replace(/[^0-9]/g, "");
      });
    });
  </script>
</MainLayout>
